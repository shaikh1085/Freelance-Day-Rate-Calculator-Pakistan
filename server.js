<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Freelance Day Rate Calculator Pakistan | PKR Hourly Rate Tool</title>
<meta name="description" content="Free freelance day rate calculator for Pakistan. Calculate hourly, daily, weekly, monthly, and project rates in PKR with expenses and tax." />
<meta name="robots" content="index,follow" />
<link rel="canonical" href="https://yourdomain.com/" />

<meta property="og:title" content="Freelance Day Rate Calculator Pakistan" />
<meta property="og:description" content="Calculate hourly, daily, weekly, monthly, and project rates in PKR." />
<meta property="og:type" content="website" />

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "What is a freelance day rate calculator?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "It is a tool that helps freelancers estimate how much to charge per day based on income goals, expenses, taxes, and working days."
      }
    },
    {
      "@type": "Question",
      "name": "Should I use PKR for Pakistan clients?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Yes, PKR is the best default currency for Pakistani freelancers working with local clients."
      }
    },
    {
      "@type": "Question",
      "name": "Why include expenses and tax?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Because your real rate should cover living costs, business costs, and taxes, not just income goals."
      }
    },
    {
      "@type": "Question",
      "name": "How many billable hours should I use?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Most freelancers use 4 to 6 billable hours per day depending on meetings, admin work, and project complexity."
      }
    }
  ]
}
</script>

<style>
:root{
  --bg:#07111f; --panel:#0f1a30; --panel2:#111f3a; --text:#eef3ff; --muted:#a9b6d6;
  --border:#233255; --accent:#7c9cff; --accent2:#35d0ba; --shadow:0 14px 40px rgba(0,0,0,.28);
}
[data-theme="light"]{
  --bg:#f5f8ff; --panel:#ffffff; --panel2:#f2f5fc; --text:#101828; --muted:#5b657a;
  --border:#d6def0; --accent:#3f67ff; --accent2:#0fbf9e; --shadow:0 14px 40px rgba(20,30,60,.12);
}
*{box-sizing:border-box}
body{margin:0;font-family:Inter,system-ui,Arial,sans-serif;background:linear-gradient(180deg,var(--bg),color-mix(in srgb, var(--bg) 92%, #0a2a4a));color:var(--text)}
.container{max-width:1120px;margin:0 auto;padding:24px}
.hero{padding:24px 0 16px}
h1{font-size:clamp(30px,5vw,54px);line-height:1.02;margin:0 0 10px}
h2{margin:0 0 10px}
p{color:var(--muted);line-height:1.7}
.grid{display:grid;grid-template-columns:1.1fr .9fr;gap:18px}
@media(max-width:950px){.grid{grid-template-columns:1fr}}
.card{background:rgba(15,26,48,.96);border:1px solid var(--border);border-radius:24px;padding:20px;box-shadow:var(--shadow)}
[data-theme="light"] .card{background:var(--panel)}
label{display:block;font-size:14px;margin:14px 0 8px;color:var(--text)}
input,select{
  width:100%;padding:14px 14px;border-radius:14px;border:1px solid var(--border);
  background:var(--panel2);color:var(--text);outline:none
}
input:focus,select:focus{border-color:var(--accent)}
.row{display:grid;grid-template-columns:1fr 1fr;gap:12px}
@media(max-width:560px){.row{grid-template-columns:1fr}}
button{
  padding:13px 16px;border:0;border-radius:14px;background:linear-gradient(135deg,var(--accent),var(--accent2));
  color:#05111f;font-weight:800;font-size:15px;cursor:pointer
}
.actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:14px}
.secondary{
  background:transparent;color:var(--text);border:1px solid var(--border)
}
.results{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:16px}
@media(max-width:560px){.results{grid-template-columns:1fr}}
.result{background:var(--panel2);border:1px solid var(--border);border-radius:18px;padding:14px}
.big{font-size:30px;font-weight:800;color:var(--text);margin-top:4px}
.small{font-size:13px;color:var(--muted)}
.list{margin:10px 0 0;padding:0;list-style:none}
.list li{padding:9px 0;border-bottom:1px solid var(--border);color:var(--text)}
.list li:last-child{border-bottom:0}
.footer{padding:22px 0 8px;color:var(--muted);font-size:13px}
.badges{display:flex;flex-wrap:wrap;gap:10px;margin-top:12px}
.badge{padding:8px 12px;border-radius:999px;background:var(--panel2);border:1px solid var(--border);color:var(--text);font-size:13px}
.section{margin-top:18px}
.kpi{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
@media(max-width:760px){.kpi{grid-template-columns:1fr}}
.kpi .box{padding:14px;border:1px solid var(--border);border-radius:18px;background:var(--panel2)}
.kpi .num{font-size:24px;font-weight:800;margin-top:6px}
</style>
</head>
<body>
<div class="container">
  <section class="hero">
    <div class="actions" style="justify-content:space-between;align-items:center">
      <div></div>
      <button class="secondary" onclick="toggleTheme()">Toggle theme</button>
    </div>
    <h1>Freelance Day Rate Calculator Pakistan</h1>
    <p>Calculate your hourly, daily, weekly, monthly, and project rate in PKR. Add expenses and tax to get a realistic number you can actually charge.</p>
    <div class="badges">
      <span class="badge">PKR calculator</span>
      <span class="badge">Live results</span>
      <span class="badge">Tax & expenses</span>
      <span class="badge">Project estimate</span>
      <span class="badge">SEO ready</span>
    </div>
  </section>

  <div class="grid">
    <section class="card">
      <h2>Calculate your rate</h2>

      <label>Monthly income goal (PKR)</label>
      <input id="income" type="number" value="150000" min="0" />

      <div class="row">
        <div>
          <label>Monthly expenses (PKR)</label>
          <input id="expenses" type="number" value="20000" min="0" />
        </div>
        <div>
          <label>Tax / buffer (%)</label>
          <input id="tax" type="number" value="10" min="0" />
        </div>
      </div>

      <div class="row">
        <div>
          <label>Working days per month</label>
          <input id="days" type="number" value="22" min="1" />
        </div>
        <div>
          <label>Billable hours per day</label>
          <input id="hours" type="number" value="6" min="1" />
        </div>
      </div>

      <div class="row">
        <div>
          <label>Project days estimate</label>
          <input id="projectDays" type="number" value="10" min="1" />
        </div>
        <div>
          <label>Number of projects per month</label>
          <input id="projectsPerMonth" type="number" value="4" min="1" />
        </div>
      </div>

      <div class="actions">
        <button onclick="copyResult()">Copy result</button>
        <button class="secondary" onclick="resetForm()">Reset</button>
      </div>

      <div class="results">
        <div class="result"><div class="small">Hourly rate</div><div class="big" id="hourly">PKR 0</div></div>
        <div class="result"><div class="small">Daily rate</div><div class="big" id="daily">PKR 0</div></div>
        <div class="result"><div class="small">Weekly rate</div><div class="big" id="weekly">PKR 0</div></div>
        <div class="result"><div class="small">Monthly target</div><div class="big" id="monthly">PKR 0</div></div>
      </div>

      <div class="section kpi">
        <div class="box"><div class="small">Project rate</div><div class="num" id="projectRate">PKR 0</div></div>
        <div class="box"><div class="small">Expense covered</div><div class="num" id="expenseCovered">PKR 0</div></div>
        <div class="box"><div class="small">Tax included</div><div class="num" id="taxIncluded">PKR 0</div></div>
      </div>

      <div class="section">
        <h2>Formula</h2>
        <p id="formulaText">Monthly target = (income goal + expenses) × (1 + tax %)</p>
      </div>
    </section>

    <aside class="card">
      <h2>Why this works</h2>
      <ul class="list">
        <li>Calculator inputs are above the fold, which users prefer for tool pages.</li>
        <li>Live results reduce friction and keep visitors engaged.</li>
        <li>Pakistan-specific PKR focus makes the page more relevant locally.</li>
        <li>FAQ schema and FAQs help Google understand the page content.</li>
        <li>Project pricing captures more search intent than rate-only tools.</li>
      </ul>

      <h2 class="section">FAQ</h2>
      <ul class="list">
        <li>What is a freelance day rate calculator?</li>
        <li>How do I set my hourly rate in Pakistan?</li>
        <li>Should I include tax and business expenses?</li>
        <li>How many billable hours should I count?</li>
      </ul>

      <div class="section">
        <h2>SEO targets</h2>
        <ul class="list">
          <li>freelance day rate calculator Pakistan</li>
          <li>hourly rate calculator Pakistan</li>
          <li>freelancer rate calculator PKR</li>
          <li>freelance income calculator Pakistan</li>
        </ul>
      </div>
    </aside>
  </div>

  <div class="footer">
    Tip: add 5–10 example scenarios and 3–5 related calculator links for stronger SEO.
  </div>
</div>

<script>
const els = {
  income: document.getElementById('income'),
  expenses: document.getElementById('expenses'),
  tax: document.getElementById('tax'),
  days: document.getElementById('days'),
  hours: document.getElementById('hours'),
  projectDays: document.getElementById('projectDays'),
  projectsPerMonth: document.getElementById('projectsPerMonth'),
  hourly: document.getElementById('hourly'),
  daily: document.getElementById('daily'),
  weekly: document.getElementById('weekly'),
  monthly: document.getElementById('monthly'),
  projectRate: document.getElementById('projectRate'),
  expenseCovered: document.getElementById('expenseCovered'),
  taxIncluded: document.getElementById('taxIncluded'),
  formulaText: document.getElementById('formulaText')
};

function fmt(v){ return 'PKR ' + Math.round(v).toLocaleString('en-PK'); }

function recalc(){
  const income = +els.income.value || 0;
  const expenses = +els.expenses.value || 0;
  const tax = +els.tax.value || 0;
  const days = Math.max(1, +els.days.value || 1);
  const hours = Math.max(1, +els.hours.value || 1);
  const projectDays = Math.max(1, +els.projectDays.value || 1);
  const projectsPerMonth = Math.max(1, +els.projectsPerMonth.value || 1);

  const base = income + expenses;
  const taxAmt = base * (tax / 100);
  const monthly = base + taxAmt;
  const daily = monthly / days;
  const hourly = daily / hours;
  const weekly = daily * 5;
  const projectRate = daily * projectDays;

  els.hourly.textContent = fmt(hourly);
  els.daily.textContent = fmt(daily);
  els.weekly.textContent = fmt(weekly);
  els.monthly.textContent = fmt(monthly);
  els.projectRate.textContent = fmt(projectRate);
  els.expenseCovered.textContent = fmt(expenses);
  els.taxIncluded.textContent = fmt(taxAmt);
  els.formulaText.textContent = `Monthly target = (income goal + expenses) × (1 + ${tax}% tax)`;
}

function copyResult(){
  const text = [
    `Hourly: ${els.hourly.textContent}`,
    `Daily: ${els.daily.textContent}`,
    `Weekly: ${els.weekly.textContent}`,
    `Monthly: ${els.monthly.textContent}`,
    `Project: ${els.projectRate.textContent}`
  ].join('\\n');
  navigator.clipboard.writeText(text);
}

function resetForm(){
  els.income.value = 150000;
  els.expenses.value = 20000;
  els.tax.value = 10;
  els.days.value = 22;
  els.hours.value = 6;
  els.projectDays.value = 10;
  els.projectsPerMonth.value = 4;
  recalc();
}

function toggleTheme(){
  const root = document.documentElement;
  root.dataset.theme = root.dataset.theme === 'light' ? 'dark' : 'light';
}

document.querySelectorAll('input').forEach(i => i.addEventListener('input', recalc));
recalc();
</script>
</body>
</html>
